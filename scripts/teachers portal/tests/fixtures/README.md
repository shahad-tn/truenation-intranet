`code.v1.gs` is the DEPLOYED code as it stood before the step 1b switch (2026-09-19), kept
only so `switch.test.js` can prove the new code behaves identically on migrated data. It is
not live anywhere and should not be edited.
